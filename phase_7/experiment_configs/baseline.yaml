# Baseline experiment configuration
<<<<<<< HEAD
# Default configuration for lighting decision evaluation

=======
>>>>>>> phase_7
experiment:
  name: baseline
  description: "Default configuration for lighting decision evaluation"
  version: "1.0"

metrics:
  consistency:
<<<<<<< HEAD
    # Intensity tolerance for determinism comparison
    # Values within ±epsilon are considered equal
    intensity_epsilon: 0.05
    enable_drift: true
    
  coverage:
    # Minimum groups expected in a valid solution
    require_minimum_groups: 2
    
  stability:
    # Number of runs for cross-run stability
    num_runs: 3

data_sources:
  # Pre-generated data paths (relative to project root)
  scenes: "data/standardized_output/*.json"
  instructions: "data/lighting_cues/*.json"

# RAG context logging policy
rag_logging:
  enabled: false
  # If enabled, log ONLY opaque identifiers:
  # - document_id
  # - chunk_id
  # NO content, embeddings, or scores allowed

output:
  # Trace output directory
  trace_dir: "data/traces/"
  # Metrics report format
=======
    intensity_epsilon: 0.05
    enable_drift: true
  coverage:
    require_minimum_groups: 2
  stability:
    num_runs: 3

data_sources:
  scenes: "data/standardized_output/*.json"
  instructions: "data/lighting_cues/*.json"

rag_logging:
  enabled: false
  # Only opaque identifiers: document_id, chunk_id

output:
  trace_dir: "data/traces/"
>>>>>>> phase_7
  report_format: "json"
